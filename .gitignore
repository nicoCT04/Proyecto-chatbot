# Secrets / environment
.env
*.env
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Logs (keep the folder, ignore contents)
logs/*
!logs/.gitkeep

# Chatbot sandbox where the agent creates demo files/repos at runtime.
# Created automatically at startup and holds a nested git repo — never tracked.
workspace/

# sugarmill SQLite database (generated from seed.py on first run)
src/servers/sugarmill/*.db

# OS / editors
.DS_Store
Thumbs.db
.idea/
.vscode/
