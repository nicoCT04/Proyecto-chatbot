"""Hand-written MCP protocol implementation (JSON-RPC 2.0 over stdio).

No MCP SDK is used here — the JSON-RPC framing, handshake and message
exchange are implemented manually, as required by the project.
"""
